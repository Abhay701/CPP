#include<iostream>
using namespace std;
int main(){
    int x1=3;
    // int 1y=2; //throws error
    int _1=3;
    cout<<x1;

// we can start name of variable with _ and $
// We can't start name of variable with numbers but can put numbers in between ot last
    // special characters except _ and $ are not allowed in variable naming
}